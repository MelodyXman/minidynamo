Project:		
	
Implements Consistent hashing with virtual_nodes of its corresponding db_node 	
	
Implements Gossip protocol in Peer to Peer cluster environtment, with pull and push type during the message transission process. Coordinator as the leader peer register and ordering the message, push to randomly chosen nodes. Other nodes can both poll and push message from/to their peers. Check the heartbeat of the nodes	
	
Implements the vector clock for time consistency in the distributed system	 	
Wanted to implement MerkleTree 80% done




